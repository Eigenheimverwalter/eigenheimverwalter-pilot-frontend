let partnerId=null;
document.addEventListener('click',event=>{const button=event.target.closest('.partner-record,.region-partner-open');if(button?.dataset.id)partnerId=button.dataset.id},true);
const escapeHtml=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
function openComposer(){
  const host=document.createElement('div');host.className='modal';host.innerHTML=`<section class="modal-card"><div class="modal-head"><h3>Partner anschreiben</h3><button class="close" type="button">×</button></div><div class="source-note">Der Versand erfolgt serverseitig und SSL-verschlüsselt über <b>partner@eigenheimverwalter.de</b> bei ALL-INKL.</div><form><label>Betreff<input name="subject" maxlength="180" required></label><label>Nachricht<textarea name="message" rows="8" maxlength="5000" required></textarea></label><button class="primary">Nachricht senden</button><p class="error" role="alert"></p></form></section>`;document.body.append(host);
  host.querySelector('.close').onclick=()=>host.remove();host.onclick=event=>{if(event.target===host)host.remove()};host.querySelector('form').onsubmit=async event=>{event.preventDefault();const error=host.querySelector('.error');error.textContent='';try{const data=Object.fromEntries(new FormData(event.target));const result=await window.ehvApi(`/api/partners/${encodeURIComponent(partnerId)}/email`,{method:'POST',body:JSON.stringify(data)});host.querySelector('.modal-card').innerHTML=`<div class="modal-head"><h3>${result.mail.status==='sent'?'E-Mail versendet':'Versand vorbereitet'}</h3><button class="close" type="button">×</button></div><p>Absender: <b>${escapeHtml(result.mail.from)}</b></p><p class="muted">${result.mail.status==='sent'?'Die Nachricht wurde verschlüsselt über ALL-INKL versendet.':'Die Nachricht liegt in der Warteschlange. Die geschützte ALL-INKL-SMTP-Konfiguration ist noch nicht vollständig hinterlegt.'}</p>`;host.querySelector('.close').onclick=()=>host.remove()}catch(reason){error.textContent=reason.message}};
}
new MutationObserver(()=>{const form=document.querySelector('#partner-edit');if(!form||document.querySelector('#email-partner')||!partnerId)return;const button=document.createElement('button');button.type='button';button.id='email-partner';button.className='outline';button.style.marginTop='10px';button.textContent='E-Mail aus partner@ senden';button.onclick=openComposer;form.insertAdjacentElement('afterend',button)}).observe(document.querySelector('#content'),{childList:true,subtree:true});
